# Application dependencies

require:
    - play 1.2.5
    - play -> crud
    - mysql -> mysql-connector-java 5.1.20
    - provided -> tika-app 1.3

repositories:
  - provided:
      type:       local
      artifact:   "${application.path}/lib/[module]-[revision].jar"
      contains:
        - provided -> *